{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Make Build",
            "command": "make && ./main", // =>
            // change executable's filename if necessary.
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Runs the project Makefile"
        }
    ]
}